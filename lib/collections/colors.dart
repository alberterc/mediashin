import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF191927);

  // Secondary colors
  static const Color secondaryColor = Color.fromARGB(255, 69, 69, 116);

  // Menu color
  static const Color primaryColorDark = Color.fromARGB(255, 15, 15, 24);

  // Surface colors
  static const Color surfaceColor = Color(0xFFFFFFFF);

  // Accent color
  static const Color accentColor = Color(0xFF99EBFF);

  // Text colors
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFFFFFFFF);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color accentText = Color(0xFF99EBFF);
}